/** The page lists either the skills on this machine or the links you published
 *  from them. Both are inventories, so they share the page's scroll and search. */
export type SkillsPageView = 'skills' | 'shared'
